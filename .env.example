# Optional. If set, the API uses Claude to write personalized "why this fits you"
# explanations. If absent, the app falls back to deterministic templated reasons,
# so the app works fully without any key.
ANTHROPIC_API_KEY=
